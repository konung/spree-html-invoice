<% buttons = Spree::Config[:print_buttons] || "invoice"
   buttons = buttons.split(",").collect{|b| b.strip } %>
<%  buttons.each do |button| %>
<%= button_link_to(I18n.t(button.to_s+".print"), "/admin/invoice/#{@order.number}/#{button}" ,
      :target => "_blank") %>
<%end%>
